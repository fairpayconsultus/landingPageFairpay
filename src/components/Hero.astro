<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-50 via-blue-100/40 to-cyan-100/50 pt-20">
  <!-- Animated Background Elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 right-20 w-96 h-96 bg-gradient-to-br from-[#10182b]/30 to-[#405687]/40 rounded-full blur-3xl animate-float-bg-large-1"></div>
    <div class="absolute bottom-20 left-20 w-96 h-96 bg-gradient-to-br from-[#a8d9fa]/40 to-[#405687]/30 rounded-full blur-3xl animate-float-bg-large-2"></div>
    
    <!-- Additional floating shapes -->
    <div class="absolute top-1/4 left-1/4 w-32 h-32 rounded-2xl bg-gradient-to-br from-[#a8d9fa]/30 to-[#405687]/20 border border-[#a8d9fa]/40 backdrop-blur-sm animate-float-hero-shape-1"></div>
    <div class="absolute bottom-1/3 right-1/4 w-24 h-24 rounded-full bg-gradient-to-br from-[#405687]/30 to-[#10182b]/20 border border-[#405687]/40 backdrop-blur-sm animate-float-hero-shape-2"></div>
    <div class="absolute top-1/2 right-1/3 w-16 h-16 rounded-xl bg-gradient-to-br from-[#10182b]/35 to-[#a8d9fa]/20 border border-[#10182b]/45 backdrop-blur-sm animate-float-hero-shape-3"></div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 relative z-10">
    <div class="text-center space-y-8">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-gradient-to-r from-[#10182b] to-[#405687] border border-[#10182b]/50 rounded-full shadow-xl shadow-[#405687]/40 animate-fade-in-up">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4 text-white"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275Z"/><path d="m5 3 1 2.5L8.5 6 6 7 5 9.5 4 7 1.5 6 4 5.5Z"/><path d="m19 17 1 2.5 2.5.5-2.5 1-1 2.5-1-2.5-2.5-1 2.5-1Z"/></svg>
        <span class="text-sm font-semibold text-white">
          Arquitectura Salarial Inteligente
        </span>
      </div>

      <!-- Main Title -->
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight animate-fade-in-up-delay-1">
        <span class="block text-gray-900">Transformamos la gestión del</span>
        <span class="block text-gray-900">talento en</span>
        <span class="block mt-2 bg-gradient-to-r from-[#10182b] via-[#405687] to-[#a8d9fa] bg-clip-text text-transparent">
          inteligencia salarial
        </span>
      </h1>

      <!-- Subtitle -->
      <p class="text-lg sm:text-xl text-gray-600 max-w-4xl mx-auto leading-relaxed animate-fade-in-up-delay-2">
        FAIRPAY convierte los procesos clave de gestión del talento en información estratégica para diseñar y gestionar estructuras salariales justas, competitivas y alineadas con los objetivos de la organización.
      </p>

      <!-- CTA Button -->
      <div class="pt-2 animate-fade-in-up-delay-3">
        <a
          href="#contacto"
          class="group inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-[#10182b] to-[#405687] text-white rounded-full text-md font-semibold hover:shadow-2xl hover:shadow-blue-500/40 transition-all duration-300 hover:scale-105"
        >
          Solicitar Demo
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4 group-hover:translate-x-1 transition-transform"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 max-w-3xl mx-auto pt-8 animate-fade-in-up-delay-4">
        {[
          { value: "80%", label: "Reducción de tiempo" },
          { value: "100%", label: "Equidad garantizada" },
          { value: "IA", label: "Análisis inteligente" },
        ].map((stat) => (
          <div class="p-6 bg-white/60 backdrop-blur-xl rounded-2xl border border-gray-200/50 shadow-lg hover:shadow-xl transition-shadow">
            <div class="text-4xl font-bold bg-gradient-to-r from-[#10182b] to-[#405687] bg-clip-text text-transparent">
              {stat.value}
            </div>
            <div class="text-gray-600 mt-2">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-scroll-indicator">
    <div class="w-6 h-10 border-2 border-[#10182b] rounded-full flex justify-center">
      <div class="w-1.5 h-3 bg-[#10182b] rounded-full mt-2 scroll-dot" />
    </div>
  </div>
</section>

<style>
  /* Entrance animations using CSS */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .animate-fade-in-up-delay-1 {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out 0.2s forwards;
  }

  .animate-fade-in-up-delay-2 {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out 0.4s forwards;
  }

  .animate-fade-in-up-delay-3 {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out 0.6s forwards;
  }

  .animate-fade-in-up-delay-4 {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out 0.8s forwards;
  }

  @keyframes scrollIndicator {
    0%, 100% {
      transform: translate(-50%, 0);
    }
    50% {
      transform: translate(-50%, 10px);
    }
  }

  .animate-scroll-indicator {
    animation: scrollIndicator 2s infinite ease-in-out;
  }

  @keyframes scrollDot {
    0%, 100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(6px);
      opacity: 0.3;
    }
  }

  .scroll-dot {
    animation: scrollDot 2s infinite ease-in-out;
  }
</style>
